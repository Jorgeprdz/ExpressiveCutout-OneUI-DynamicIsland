package com.ekoehler.expressivecutout.overlay

/** Resting visibility state of the persistent LiveActivity container. */
internal enum class LiveActivityContainerState {
    HIDDEN,
    COLLAPSED,
    EXPANDED,
}
