package com.ekoehler.expressivecutout.overlay

/** Existing renderer family selected for one neutral LiveActivity. */
internal enum class LiveActivityRenderKind {
    CALL,
    TIMER,
    MUSIC,
    GENERIC_PROGRESS,
    GENERIC_LIVE,
}
